<div class="container d-flex flex-column align-items-center mt-4">
  <h1><%= @movie.title %></h1>
  <p> <%= @movie.overview %></p>
  <img src="<%= @movie.poster_url %>" alt="poster of <%= @movie.title %>" class='w-25'>
  <p>Rating: <strong><%= @movie.rating %></strong></p>
</div>
